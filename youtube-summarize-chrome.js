javascript:(async function(){async function fetchTranscript(){const visibleSegments=document.querySelectorAll('ytd-transcript-segment-renderer yt-formatted-string.segment-text');if(visibleSegments.length>0){return Array.from(visibleSegments).map(el=>el.textContent.trim()).join('\n')}else{throw new Error('No transcript found. Please:\n1. Click the three dots (...) below the video\n2. Click "Show transcript"\n3. Wait for transcript to load\n4. Run this bookmarklet again')}}async function getSummaryFromDeepSeek(transcript){const messages=[{role:"system",content:"You are a helpful assistant that summarizes text."},{role:"user",content:`Summarize the following text to ~100 words:\n\n${transcript}`}];const truncated=messages[1].content.slice(0,12000);messages[1].content=truncated;const body={model:"deepseek-chat",messages};const res=await fetch("https://api.deepseek.com/v1/chat/completions",{method:"POST",headers:{"Content-Type":"application/json","Authorization":"Bearer sk-NUMBER"},body:JSON.stringify(body)});if(!res.ok){const errorText=await res.text();throw new Error(`DeepSeek API error: ${res.status} - ${errorText}`)}const data=await res.json();return data.choices?.[0].message?.content||"No summary found."}try{const transcript=await fetchTranscript();const summary=await getSummaryFromDeepSeek(transcript);try{await navigator.clipboard.writeText(summary);alert(`Summary (copied to clipboard):\n\n${summary}`)}catch(err){alert("Summary:\n\n"+summary)}}catch(err){alert(`Error: ${err.message}`);console.error(err)}})();
